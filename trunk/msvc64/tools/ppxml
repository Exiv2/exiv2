#!/usr/bin/perl

use XML::Twig;
use XML::Parser;

# cpan XML::{Twig,Parser} to install

my $xml = XML::Twig->new(pretty_print => 'indented');

$xml->parse(\*STDIN);
$xml->print();

# That's all Folks!
##
